{{#animated-value predicate key=key rules=rules use=use duration=duration group=realGroup initialInsertion=initialInsertion as |currentPredicate|}}
  {{#if currentPredicate}}
    {{yield}}
  {{else}}
    {{yield to="inverse"}}
  {{/if}}
{{/animated-value}}

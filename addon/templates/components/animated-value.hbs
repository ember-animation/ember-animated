{{#animated-each items key=key rules=rules use=use duration=duration group=group watch=watch initialInsertion=initialInsertion finalRemoval=finalRemoval as |item|}}
  {{yield item}}
{{/animated-each}}

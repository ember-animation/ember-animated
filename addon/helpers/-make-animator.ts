import { helper } from '@ember/component/helper';
import Component from '@glimmer/component';

export function makeAnimator(_params: any[], hash: { sprites: Component }) {
  debugger;
  return hash;
}

export default helper(makeAnimator);

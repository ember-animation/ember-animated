{{!--
  The @class is only there to support a deprecated usage.
--}}
{{#let (component (ensure-safe-component (-element this.tag))) as |Tag|~}}
  <Tag class="animated-container {{@class}}" ...attributes >
    {{yield}}
  </Tag>
{{/let}}
<div class="hero-list">
  {{#each model as |person|}}
    {{#link-to "hero.detail" person.id tagName="div"}}
      {{#animated-bind person rules=rules as |p|}}
        <img src={{p.avatarUrl}}>
      {{/animated-bind}}
      <p>{{person.name}}</p>
    {{/link-to}}
  {{/each}}
</div>
